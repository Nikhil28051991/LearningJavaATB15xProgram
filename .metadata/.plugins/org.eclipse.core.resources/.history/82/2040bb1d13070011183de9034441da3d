package com.everycred.automation;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import io.github.bonigarcia.wdm.WebDriverManager;

public class EveryCredLogin {

    public static void main(String[] args) {

        WebDriverManager.chromedriver().setup();
        WebDriver driver = new ChromeDriver();
        driver.manage().window().maximize();

        WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(60));
        JavascriptExecutor js = (JavascriptExecutor) driver;
        Actions actions = new Actions(driver);

        try {

            // ================= LOGIN =================
            driver.get("https://demo-dcs-issuer-us.everycred.com/auth/login");

            wait.until(ExpectedConditions.visibilityOfElementLocated(By.id("email")))
                    .sendKeys("admin.albuquerque@yopmail.com");

            driver.findElement(By.cssSelector("input[type='password']"))
                    .sendKeys("Everycred@123");

            WebElement loginBtn = driver.findElement(By.xpath("//button[@type='submit']"));
            js.executeScript("arguments[0].click();", loginBtn);

            // Wait until Subjects menu appears (login success)
            WebElement subjectsMenu = wait.until(
                    ExpectedConditions.visibilityOfElementLocated(
                            By.xpath("//*[normalize-space()='Subjects']")));

            System.out.println("✅ Login successful");

            // ================= CLICK SUBJECTS =================
            subjectsMenu.click();

            // ================= CLICK CREATE NEW =================
            WebElement createNewBtn = wait.until(
                    ExpectedConditions.elementToBeClickable(
                            By.xpath("//button[contains(.,'Create New')]")));

            createNewBtn.click();

            // Wait for dropdown
            wait.until(ExpectedConditions.visibilityOfElementLocated(
                    By.xpath("//div[contains(@class,'p-menu')]")));

            // ================= CLICK CREATE GROUP =================
            WebElement createGroupOption = wait.until(
                    ExpectedConditions.elementToBeClickable(
                            By.xpath("//*[normalize-space()='Create Group']")));

            actions.moveToElement(createGroupOption).click().perform();

            // ================= WAIT FOR DIALOG =================
            WebElement dialog = wait.until(
                    ExpectedConditions.visibilityOfElementLocated(
                            By.xpath("//div[contains(@class,'p-dialog')]")));

            System.out.println("✅ Create Group dialog opened");

            // ================= ENTER GROUP NAME =================
            WebElement groupNameInput = wait.until(
                    ExpectedConditions.visibilityOfElementLocated(
                            By.xpath("(//div[contains(@class,'p-dialog')]//input[@type='text'])[1]")));

            groupNameInput.clear();
            groupNameInput.sendKeys("Employee ID Card");

            System.out.println("✅ Group name entered");

            // ================= CLICK CREATE BUTTON =================
            WebElement createBtn = wait.until(
                    ExpectedConditions.elementToBeClickable(
                            By.xpath("//div[contains(@class,'p-dialog')]//button[contains(.,'Create')]")));

            createBtn.click();

            System.out.println("✅ Group created successfully");

            // Wait for dialog to close
            wait.until(ExpectedConditions.invisibilityOf(dialog));

            // ================= CREATE SUBJECT =================
            createNewBtn = wait.until(
                    ExpectedConditions.elementToBeClickable(
                            By.xpath("//button[contains(.,'Create New')]")));

            createNewBtn.click();

            wait.until(ExpectedConditions.visibilityOfElementLocated(
                    By.xpath("//div[contains(@class,'p-menu')]")));

            WebElement createSubjectOption = wait.until(
                    ExpectedConditions.elementToBeClickable(
                            By.xpath("//*[normalize-space()='Create Subject']")));

            actions.moveToElement(createSubjectOption).click().perform();

            System.out.println("✅ Create Subject opened successfully");

            Thread.sleep(15000);

        } catch (Exception e) {
            e.printStackTrace();
        }

        // driver.quit();
    }
}
